# دليل الاستخدام - نظام تسجيل الدخول والجلسات

## 🔐 نظام المصادقة والجلسات

تم إضافة نظام مصادقة متكامل مع إدارة الجلسات للمشروع.

### الملفات الجديدة:

1. **`frontend/login.html`** - صفحة تسجيل الدخول
2. **`frontend/js/session.js`** - إدارة الجلسات والمصادقة
3. **Backend endpoints جديدة**:
   - `GET /api/v1/auth/verify` - التحقق من صحة الـ token
   - `POST /api/v1/auth/refresh` - تجديد الـ token

## 🚀 كيفية الاستخدام:

### 1. تشغيل النظام:
```bash
# تشغيل سريع
start.bat

# أو يدوياً
npm install
npm run dev
```

### 2. الوصول للنظام:
- **صفحة تسجيل الدخول**: `frontend/login.html`
- **التطبيق الرئيسي**: `frontend/index.html` (يتطلب تسجيل دخول)

### 3. بيانات الدخول الافتراضية:

| المستخدم | كلمة المرور | الصلاحيات |
|----------|-------------|-----------|
| admin | Admin@123 | جميع الصلاحيات |
| manager | Admin@123 | إدارة المنتجات والمبيعات |
| cashier | Admin@123 | المبيعات والمرتجعات فقط |

## ✨ المميزات الجديدة:

### 1. صفحة تسجيل الدخول:
- ✅ تصميم عصري مع Glass Morphism
- ✅ تسجيل دخول سريع بأزرار جاهزة
- ✅ رسائل خطأ واضحة
- ✅ حالات تحميل متقدمة
- ✅ دعم كامل للغة العربية

### 2. إدارة الجلسات:
- ✅ **حفظ الجلسة**: تبقى مسجل دخولك حتى تسجل الخروج
- ✅ **التحقق التلقائي**: فحص صحة الجلسة عند فتح التطبيق
- ✅ **تجديد تلقائي**: تحديث الـ token قبل انتهاء صلاحيته
- ✅ **إعادة توجيه ذكية**: توجيه تلقائي لصفحة تسجيل الدخول عند انتهاء الجلسة

### 3. نظام الصلاحيات:
- ✅ **صلاحيات متدرجة**: كل دور له صلاحيات محددة
- ✅ **إخفاء العناصر**: إخفاء الأزرار والقوائم حسب الصلاحيات
- ✅ **حماية الـ APIs**: جميع الطلبات محمية بالـ token

### 4. واجهة المستخدم:
- ✅ **قائمة المستخدم**: عرض اسم المستخدم والدور
- ✅ **زر تسجيل الخروج**: في قائمة المستخدم
- ✅ **إشعارات**: رسائل نجاح وخطأ واضحة

## 🔧 للمطورين:

### استخدام SessionManager:
```javascript
// التحقق من تسجيل الدخول
if (sessionManager.isAuthenticated()) {
    // المستخدم مسجل دخوله
}

// الحصول على بيانات المستخدم الحالي
const user = sessionManager.getCurrentUser();

// التحقق من الصلاحيات
if (sessionManager.hasRole('admin')) {
    // المستخدم مدير
}

if (sessionManager.canPerformAction('manage_products')) {
    // يمكن إدارة المنتجات
}

// تسجيل الخروج
sessionManager.logout();
```

### إضافة صلاحيات للعناصر:
```html
<!-- إخفاء العنصر للأدوار غير المصرح لها -->
<button data-roles="admin,manager">إدارة المنتجات</button>

<!-- إخفاء العنصر للصلاحيات غير المتاحة -->
<div data-permission="manage_sales">قسم المبيعات</div>
```

### استخدام APIManager المحدث:
```javascript
// جميع الطلبات تستخدم الـ token تلقائياً
const response = await dataManager.api.request('products');

// في حالة انتهاء الجلسة، سيتم إعادة التوجيه تلقائياً
```

## 🛡️ الأمان:

### 1. حماية الـ Token:
- تخزين آمن في localStorage
- التحقق من صحة الـ token مع كل طلب
- إزالة تلقائية عند انتهاء الصلاحية

### 2. حماية الصفحات:
- فحص تلقائي للمصادقة عند تحميل الصفحة
- إعادة توجيه فورية للمستخدمين غير المصادقين
- حماية جميع الـ API endpoints

### 3. إدارة الجلسات:
- انتهاء صلاحية تلقائي
- تحذير قبل انتهاء الجلسة
- تجديد آمن للـ tokens

## 📱 تجربة المستخدم:

### 1. تسجيل الدخول:
1. افتح `frontend/login.html`
2. أدخل بيانات الدخول أو استخدم الأزرار السريعة
3. سيتم توجيهك للتطبيق الرئيسي تلقائياً

### 2. استخدام التطبيق:
- جميع الوظائف متاحة حسب صلاحياتك
- الجلسة محفوظة حتى تسجل الخروج
- إشعارات واضحة لجميع العمليات

### 3. تسجيل الخروج:
1. اضغط على صورة المستخدم في الأعلى
2. اختر "تسجيل الخروج"
3. سيتم توجيهك لصفحة تسجيل الدخول

## 🎯 النتيجة:

✅ **نظام مصادقة كامل** مع حفظ الجلسات  
✅ **أمان متقدم** مع صلاحيات متدرجة  
✅ **تجربة مستخدم ممتازة** مع واجهة عصرية  
✅ **سهولة الاستخدام** مع تسجيل دخول سريع  
✅ **حماية شاملة** لجميع الـ APIs  

**الآن يمكنك تجربة جميع الـ APIs براحة تامة مع نظام جلسات محفوظ! 🚀**