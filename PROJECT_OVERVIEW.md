# منظومة إدارة بيع الإكسسوارات ومواد التجميل

## نظرة عامة
هذا المشروع هو `Backend API` متكامل لإدارة بيع الإكسسوارات ومواد التجميل باستخدام:

- `Node.js`
- `Express.js`
- `MySQL`

الهدف من المشروع هو بناء منظومة واضحة، عامة، وقابلة للتوسع لإدارة دورة حياة جميع العناصر الأساسية داخل النشاط التجاري، مثل المنتجات، الموردين، الموظفين، العملاء، المبيعات، المشتريات، والمخزون، مع وجود سجل موحد يوثق أي حركة أو تغيير يحدث في النظام.

الفكرة المحورية في هذا المشروع هي أن كل كيان مهم داخل المنظومة لا يقتصر على مجرد بيانات ثابتة، بل يمتلك دورة حياة كاملة يمكن تتبعها من خلال سجل عمليات واضح ودقيق.

---

## هدف المشروع
يهدف المشروع إلى توفير `RESTful API` منظم وبسيط لإدارة:

- المنتجات
- التصنيفات
- الموردين
- الموظفين
- العملاء
- المبيعات
- المشتريات
- المخزون
- سجل الحركات `Activity Log / Audit Trail`

كما يهدف إلى:

- فصل واضح بين الطبقات البرمجية.
- تسهيل التوسع المستقبلي.
- توفير بنية مناسبة للصيانة.
- توثيق الـ APIs بشكل رسمي.
- تمكين التتبع الكامل لجميع العمليات داخل النظام.

---

## فكرة النظام
النظام مبني على مبدأ أن كل عملية مهمة يجب أن تُسجل داخل سجل مركزي، بحيث يمكن الرجوع لاحقًا لمعرفة:

- من قام بالعملية
- متى تمت العملية
- على أي كيان تمت
- ما نوع التغيير الذي حدث
- ما هي البيانات المرتبطة بهذه الحركة

أمثلة على الحركات التي يجب تسجيلها:

- إنشاء منتج جديد
- تعديل بيانات منتج
- تغيير سعر البيع أو التكلفة
- إضافة كمية للمخزون
- خصم كمية بسبب عملية بيع
- إنشاء فاتورة شراء
- إنشاء فاتورة بيع
- إلغاء فاتورة
- إضافة مورد أو تعديل بياناته
- إضافة موظف أو تغيير حالته

---

## الكيانات الأساسية في النظام

### 1. المنتجات `Products`
تمثل جميع العناصر التي يتم بيعها مثل الإكسسوارات أو مواد التجميل.

البيانات الأساسية المتوقعة:

- الاسم
- الكود
- الباركود
- الوصف
- التصنيف
- سعر الشراء
- سعر البيع
- الكمية الحالية
- الحد الأدنى للمخزون
- الحالة

### 2. التصنيفات `Categories`
لتقسيم المنتجات إلى مجموعات مثل:

- مكياج
- عطور
- عناية بالبشرة
- إكسسوارات

### 3. الموردون `Suppliers`
الأطراف التي يتم شراء المنتجات منهم.

البيانات الأساسية:

- الاسم
- رقم الهاتف
- البريد الإلكتروني
- العنوان
- الملاحظات
- الحالة

### 4. الموظفون `Employees`
الأشخاص الذين يستخدمون النظام أو ينفذون العمليات داخله.

البيانات الأساسية:

- الاسم
- رقم الهاتف
- الوظيفة
- الراتب
- الحالة
- تاريخ التعيين

### 5. العملاء `Customers`
العملاء المرتبطون بعمليات البيع عند الحاجة.

### 6. المشتريات `Purchases`
فواتير شراء المنتجات من الموردين.

### 7. المبيعات `Sales`
فواتير بيع المنتجات للعملاء.

### 8. حركات المخزون `Inventory Transactions`
أي زيادة أو نقصان أو تعديل على كمية منتج داخل المخزون.

### 9. سجل الحركات `Activity Logs`
سجل عام وموحد لأي عملية تحدث داخل النظام.

---

## دورة حياة العناصر داخل المنظومة
كل عنصر داخل النظام يجب أن يكون له سجل تاريخي للحركات المرتبطة به.

### مثال على دورة حياة المنتج

1. إنشاء المنتج
2. ربطه بتصنيف
3. ربطه بمورد أو أكثر بشكل غير مباشر عبر المشتريات
4. إضافة كمية أولية
5. تعديل السعر أو البيانات
6. تنفيذ عمليات بيع تخصه
7. تنفيذ تسويات مخزون أو مرتجعات
8. إيقافه أو تعطيله

كل خطوة من هذه الخطوات يجب أن تظهر داخل سجل الحركة.

### مثال على دورة حياة المورد

1. إنشاء المورد
2. تعديل بيانات الاتصال
3. تنفيذ عمليات شراء منه
4. تقييم أو تعطيل المورد

### مثال على دورة حياة الموظف

1. إضافة الموظف
2. تحديد دوره أو وظيفته
3. تنفيذ عمليات بواسطة حسابه
4. تغيير حالته إلى نشط أو موقوف

---

## المبدأ المعماري للمشروع
سيتم بناء المشروع بأسلوب طبقات واضحة وبسيطة، حتى يكون المشروع سهل الفهم والتطوير.

الطبقات الأساسية هي:

- `models`
- `controllers`
- `services`
- `routes`
- `middleware`
- `utils`

كل طبقة لها مسؤولية مستقلة.

---

## البنية الهيكلية المقترحة للمشروع

```text
beauty-accessories-backend/
├─ src/
│  ├─ config/
│  │  ├─ db.js
│  │  ├─ env.js
│  │  └─ swagger.js
│  │
│  ├─ models/
│  │  ├─ product.model.js
│  │  ├─ category.model.js
│  │  ├─ supplier.model.js
│  │  ├─ employee.model.js
│  │  ├─ customer.model.js
│  │  ├─ purchase.model.js
│  │  ├─ purchaseItem.model.js
│  │  ├─ sale.model.js
│  │  ├─ saleItem.model.js
│  │  ├─ inventoryTransaction.model.js
│  │  ├─ activityLog.model.js
│  │  └─ user.model.js
│  │
│  ├─ controllers/
│  │  ├─ product.controller.js
│  │  ├─ category.controller.js
│  │  ├─ supplier.controller.js
│  │  ├─ employee.controller.js
│  │  ├─ customer.controller.js
│  │  ├─ purchase.controller.js
│  │  ├─ sale.controller.js
│  │  ├─ inventory.controller.js
│  │  ├─ activityLog.controller.js
│  │  └─ auth.controller.js
│  │
│  ├─ services/
│  │  ├─ product.service.js
│  │  ├─ supplier.service.js
│  │  ├─ employee.service.js
│  │  ├─ customer.service.js
│  │  ├─ purchase.service.js
│  │  ├─ sale.service.js
│  │  ├─ inventory.service.js
│  │  ├─ activityLog.service.js
│  │  └─ auth.service.js
│  │
│  ├─ routes/
│  │  ├─ index.js
│  │  ├─ product.routes.js
│  │  ├─ category.routes.js
│  │  ├─ supplier.routes.js
│  │  ├─ employee.routes.js
│  │  ├─ customer.routes.js
│  │  ├─ purchase.routes.js
│  │  ├─ sale.routes.js
│  │  ├─ inventory.routes.js
│  │  ├─ activityLog.routes.js
│  │  └─ auth.routes.js
│  │
│  ├─ middleware/
│  │  ├─ auth.middleware.js
│  │  ├─ role.middleware.js
│  │  ├─ validate.middleware.js
│  │  ├─ error.middleware.js
│  │  └─ notFound.middleware.js
│  │
│  ├─ utils/
│  │  ├─ apiResponse.js
│  │  ├─ asyncHandler.js
│  │  ├─ appError.js
│  │  ├─ generateCode.js
│  │  ├─ pagination.js
│  │  └─ logger.js
│  │
│  ├─ validators/
│  │  ├─ product.validator.js
│  │  ├─ supplier.validator.js
│  │  ├─ employee.validator.js
│  │  ├─ customer.validator.js
│  │  ├─ purchase.validator.js
│  │  ├─ sale.validator.js
│  │  └─ auth.validator.js
│  │
│  ├─ docs/
│  │  ├─ swagger.yaml
│  │  └─ swaggerSchemas.js
│  │
│  ├─ app.js
│  └─ server.js
│
├─ database/
│  ├─ schema.sql
│  ├─ seed.sql
│  └─ migrations/
│
├─ .env
├─ .env.example
├─ .gitignore
├─ package.json
└─ README.md
```

---

## شرح الطبقات

### `models`
تتعامل مع قاعدة البيانات مباشرة، وتحتوي على الاستعلامات أو وظائف الوصول إلى البيانات.

### `controllers`
تستقبل الطلبات من الـ routes، وتتعامل مع `request` و`response` فقط، ثم تمرر التنفيذ إلى طبقة الخدمات.

### `services`
هذه أهم طبقة في النظام، لأنها تحتوي على منطق الأعمال الفعلي مثل:

- تنفيذ عملية بيع
- تنفيذ عملية شراء
- تحديث المخزون
- تسجيل الحركات
- التحقق من القيود التجارية

### `routes`
مسؤولة عن تعريف المسارات وربطها بالـ controllers.

### `middleware`
تضم الوظائف الوسيطة مثل:

- التحقق من التوكن
- الصلاحيات
- التحقق من البيانات
- معالجة الأخطاء

### `utils`
تحتوي على الأدوات المساعدة العامة القابلة لإعادة الاستخدام.

### `docs`
تضم ملفات توثيق الـ API باستخدام `Swagger / OpenAPI`.

---

## تدفق الطلب داخل المشروع

```text
Client Request
-> Route
-> Controller
-> Service
-> Model
-> MySQL
-> Service
-> Controller
-> API Response
```

وفي العمليات المهمة مثل البيع والشراء:

```text
Create Sale / Purchase
-> Validate input
-> Save invoice
-> Save invoice items
-> Update inventory
-> Save inventory transaction
-> Save activity log
-> Return response
```

---

## قاعدة البيانات
بيانات الاتصال التي سيتم اعتمادها مبدئيًا:

- `host`: `localhost`
- `port`: `3306`
- `user`: `root`
- `password`: `12255`

اسم قاعدة البيانات المقترح:

`beauty_accessories_system`

تم اختيار هذا الاسم لأنه واضح، عام، ويمثل طبيعة المشروع بشكل مناسب.

---

## سجل الحركات المركزي
هذا الجزء من أهم أجزاء المنظومة.

كل عملية تتم على أي كيان مهم يجب أن تنتج سجلًا داخل جدول مثل `activity_logs`.

الحقول المتوقعة:

- `id`
- `entity_type`
- `entity_id`
- `action`
- `description`
- `performed_by`
- `metadata`
- `created_at`

أمثلة على القيم:

- `entity_type = product`
- `entity_id = 15`
- `action = stock_added`
- `description = Added 20 units through purchase invoice`

ومن خلال هذا السجل يمكن استخراج التاريخ الكامل لأي عنصر في النظام.

---

## حركات المخزون
لأن المخزون حساس ويؤثر مباشرة على المبيعات والمشتريات، يجب إنشاء سجل مستقل لحركات المخزون أيضًا.

أمثلة على أنواع الحركة:

- `purchase_in`
- `sale_out`
- `adjustment_add`
- `adjustment_remove`
- `return_in`
- `return_out`

هذا يسهل معرفة مصدر أي تغيير في الكمية.

---

## الـ API Documentation
يجب أن يتضمن المشروع توثيقًا رسميًا للـ API باستخدام `Swagger/OpenAPI`.

الهدف من التوثيق:

- شرح جميع الـ endpoints
- توضيح شكل الطلبات والاستجابات
- توضيح الأكواد الخاصة بالأخطاء
- تسهيل الاختبار من خلال واجهة تفاعلية

المسار المقترح للتوثيق:

`/api/docs`

والمسارات الأساسية المقترحة:

- `/api/v1/auth`
- `/api/v1/products`
- `/api/v1/categories`
- `/api/v1/suppliers`
- `/api/v1/employees`
- `/api/v1/customers`
- `/api/v1/purchases`
- `/api/v1/sales`
- `/api/v1/inventory`
- `/api/v1/activity-logs`

---

## مبادئ مهمة في التنفيذ

### 1. البساطة
الهيكلية يجب أن تبقى عامة وسهلة الفهم، بدون تعقيد زائد.

### 2. الفصل بين المسؤوليات
لا يتم وضع منطق الأعمال داخل `routes` أو `controllers`.

### 3. قابلية التوسع
يجب أن يكون من السهل إضافة:

- صلاحيات
- تقارير
- إشعارات
- مرتجعات
- تعدد فروع

### 4. الاعتمادية
عمليات البيع والشراء وتحديث المخزون يفضل تنفيذها داخل `database transactions`.

### 5. التتبع الكامل
أي عملية مهمة يجب أن تسجل في:

- `inventory_transactions` إذا أثرت على الكمية
- `activity_logs` إذا أثرت على أي كيان في النظام

---

## النتيجة النهائية المتوقعة من هذا المشروع
عند اكتمال المشروع، سيكون لدينا `Backend API` منظم قادر على:

- إدارة المنتجات والتصنيفات
- إدارة الموردين والموظفين والعملاء
- تسجيل عمليات الشراء والبيع
- تحديث المخزون تلقائيًا
- تتبع جميع الحركات داخل النظام
- تقديم APIs موثقة رسميًا
- العمل كأساس قوي لتطوير لوحة تحكم أو تطبيق موبايل لاحقًا

---

## الخطوة التالية
الخطوة المنطقية التالية بعد هذه الوثيقة هي:

1. إنشاء هيكل المشروع فعليًا.
2. إعداد `Express` و`MySQL connection`.
3. إنشاء ملفات الأساس داخل `src`.
4. كتابة `schema.sql`.
5. تجهيز `Swagger`.
6. البدء بأول الوحدات الأساسية مثل:
   - المنتجات
   - الموردين
   - المبيعات
   - سجل الحركات

